using React we are creaing below features : 

# This Application is divided up in the following sections:

1 Setting up the project
2 Adding users table
3 Adding a user
4 Deleting a user
5 Updating a user
6 Using the Effect Hook
7 Bonus: fetching users from an API


How to clone this into our Machine : 

$ git clone https://github.com/pankaj1404/crudApp.git

# or create a new repository on the command line
echo "# crudApp" >> README.md
git init
git add README.md
git commit -m "initial commit"
git branch -M main
git remote add origin https://github.com/pankaj1404/crudApp.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/pankaj1404/crudApp.git
git branch -M main
git push -u origin main


